# Anmol Digital Seva — V2
Upload the files to the root of a GitHub repository. Then enable GitHub Pages from Settings → Pages → Deploy from branch → main → root.

V2 fixes the broken social icons by embedding the PNG icons directly in the HTML, uses exactly two separate poster images, and improves the mobile header/logo and popup behavior.
